const config = {
  name: 'development',
  slack: {
    client_id: 'secrets@slack.client_id',
    client_secret: 'secrets@slack.client_secret',
    token: 'secrets@slack.token',
  },
};

module.exports = config;
